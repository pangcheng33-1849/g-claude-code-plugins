# 诊断顺序

除非错误原因已经非常明显，否则按下面顺序检查：

1. 是否具备应用凭证
2. 是否选择了正确的 token 类型
3. scope 是否大概率足够
4. endpoint 路径是否正确
5. 标识符格式是否匹配对象类型
6. payload 字段是否合法
7. 分页、限流或重试行为是否异常

这个顺序可以避免在真正问题是身份或 scope 时，把时间浪费在 payload 细节上。
